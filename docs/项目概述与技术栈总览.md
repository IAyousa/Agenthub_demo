# AgentHub 技术框架文档 — 项目概述与技术栈总览

> **版本**: v1.0  
> **最后更新**: 2026-06-05  
> **文档用途**: 团队开发统一参考 + AI 编程工具上下文注入

---

## 1. 项目概述

### 1.1 项目名称

**AgentHub** — 多 Agent 协作平台

### 1.2 核心功能一句话描述

以 IM 聊天为交互界面，通过主 Agent（Orchestrator）调度多个 AI Agent，在聊天流中生成、预览、部署代码/网页等产物的协作平台。

### 1.3 核心用户场景

- 用户创建聊天会话，选择 Agent（如 Claude Code）
- 通过 WebSocket 实时发送消息给后端
- 后端通过 Agent 网关调用 Python Agent 服务
- Agent 返回流式结果，逐字展示在聊天窗口
- 产物（代码/网页）内联预览，支持编辑和部署

### 1.4 团队画像

| 维度 | 现状 |
|------|------|
| 团队规模 | 4 人，CS 大三学生 |
| 主力语言 | Java，Spring Boot 有实战经验 |
| Python | 了解基础，能借助 AI 编程工具开发 |
| Agent 知识 | 2 人有理论知识，全队零实战经验 |
| WebSocket | 零经验 |
| AI 编程工具 | 可用（Cursor / Copilot） |

---

## 2. 技术栈总览

### 2.1 技术栈矩阵

#### 前端

| 技术 | 实际版本 | 选型理由 |
|------|---------|---------|
| Vue 3 | 3.5+ | 团队熟悉，响应式数据驱动，适合实时交互 |
| Vite | 8.x | 快速热更新，开发体验好，原生支持 Tailwind CSS 4 |
| TypeScript | 6.x | 全量 TypeScript 类型安全，团队已适应 |
| Pinia | 3.x | Vue 3 官方推荐状态管理，简洁直观 |
| Vue Router | 4.6+ | SPA 页面路由，History 模式驱动 URL 为单一真相来源 |
| STOMP.js + SockJS | ^7.0.0 | 与 Spring Boot WebSocket 完美对接 |
| Monaco Editor | ^0.55.0 | VS Code 内核，支持语法高亮和 Diff |
| Tailwind CSS 4 | 4.x | 原子化 CSS，快速构建高度定制化 IM UI |
| GSAP | 3.x | SVG 办公室场景动画（人物走路/踢人动画） |
| UI 组件库 | 无（手写组件） | IM 聊天 UI 高度定制，组件库反而不灵活 |

#### 后端主服务

| 技术 | 版本要求 | 选型理由 |
|------|---------|---------|
| Spring Boot | 3.2.x | 团队主力语言，WebSocket 支持成熟 |
| Java | 17+ | 团队舒适区 |
| Spring Data JPA | - | 自动建表，减少 SQL 编写 |
| spring-boot-starter-websocket | - | 官方 STOMP 支持 |
| WebClient (WebFlux) | - | 异步非阻塞，支持 SSE 流式读取 |
| PostgreSQL JDBC Driver | - | 连接 PostgreSQL |

#### Agent 服务

| 技术 | 版本要求 | 选型理由 |
|------|---------|---------|
| FastAPI | 0.109.x | Python AI 生态原生支持 |
| Python | 3.11+ | LangChain 等框架的唯一语言选择 |
| LangChain + LangGraph | 0.1.x | 构建 Orchestrator 调度逻辑（P2 阶段） |
| httpx | 0.26.x | 异步 HTTP 客户端，用于自定义 Agent 的 HTTP API 调用 |
| Claude Code CLI | latest | Anthropic 官方本地 Agent CLI，支持文件读写和 Shell 执行 |
| OpenAI Codex CLI | latest | OpenAI 官方本地 Agent CLI，支持文件读写和 Shell 执行 |
| asyncio (标准库) | Python 3.11+ | 异步子进程管理，驱动 CLI Agent 的流式输出 |

#### 基础设施

| 组件 | 版本 | 用途 |
|------|------|------|
| H2 Database | 2.x | MVP 阶段文件模式持久化，无需额外安装数据库服务 |
| Docker + Docker Compose | latest | P1 阶段 PostgreSQL 统一部署（MVP 可选） |
| 本地文件系统 | - | MVP 阶段产物存储 ./artifacts |
| Java 内存 Map | - | MVP 阶段 WebSocket 会话管理（P1 阶段迁至 Redis） |
| 无独立数据库进程 | - | MVP 零部署成本，直接启动即可运行 |

### 2.2 MVP 阶段简化策略

| 组件 | MVP 阶段（当前实际） | P1 阶段 |
|------|---------|---------|
| 数据库 | **H2 文件模式（零部署）** | PostgreSQL |
| 缓存/会话管理 | **Java 内存 ConcurrentHashMap** | Redis（可选） |
| 文件存储 | **本地文件系统 ./artifacts + Spring 静态资源映射** | MinIO |
| Agent 调用方式 | **CLI 子进程（Claude Code / Codex）**，custom 占位待实现 | 同左 + 自定义 Agent HTTP API（http_adapter） |
| Python Agent 服务职责 | **纯无状态网关，零数据库访问** | 同左 + Orchestrator 调度逻辑 |

### 2.3 选型核心原则

1. **团队舒适区优先**：工程侧用 Java/Spring Boot，不强制学习新语言
2. **AI 生态对齐**：Agent 侧用 Python，LangChain 生态的所有教程/示例直接可用
3. **渐进式复杂度**：MVP 用最简单的方案跑通链路，P1/P2 逐步引入高级组件
4. **接口契约解耦**：Java 和 Python 通过 HTTP 通信，各自独立开发测试
5. **数据单一权威**：Java 后端是唯一的数据持久层，Python Agent 服务是纯无状态网关，零数据库访问，职责边界绝对清晰

### 2.4 当前项目完成度快照
| 层级 | 完成度 | 核心状态 |
|------|--------|---------|
| 前端 Vue 3 | ~92% | 3 列布局、路由系统、STOMP WebSocket、REST API 全部就绪；产物预览（preview_card→ArtifactSandbox iframe）完整链路可跑通 |
| 后端 Spring Boot | ~70% | 数据层/Service/Controller/WebSocket 全部就绪；/internal/artifacts 内部端点已接线（Python→Java上传→preview_card推送）；Agent 会话级工作区隔离已实现 |
| Agent FastAPI | ~85% | CLI 适配器+AGENT_REGISTRY+增强提示词全就绪；artifact_uploader.py 自动检测代码块并上传；会话级工作区隔离（./agent_workspaces/{convId}）；`http_adapter` 和 `orchestrator.py` 待实现 |